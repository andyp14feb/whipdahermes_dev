# VITE_API_BASE_URL intentionally omitted.
# During dev, Vite's built-in proxy handles /machines, /sessions, /commands
# and forwards them to the API server on the same origin — no CORS needed.
# For production builds, set this to the production API URL.
